# SPDX-License-Identifier: MIT
# WasmTeX original patch (see the sibling HEADER.md for what/why/upstream-able).
# The diff's *changes* are original WasmTeX work, not derived from any GPL/AGPL
# source. The context lines below necessarily quote small excerpts of the
# patched zlib source, which remain under the zlib license — the
# diff-context-excerpt licensing clause is stated in this patch's HEADER.md.
# `patch` ignores this leading comment; it applies the diff from the '--- ' line.
--- a/libs/zlib/zlib-src/zutil.h
+++ b/libs/zlib/zlib-src/zutil.h
@@ -137,7 +137,7 @@
 #  endif
 #endif
 
-#if defined(MACOS) || defined(TARGET_OS_MAC)
+#if defined(MACOS)
 #  define OS_CODE  7
 #  ifndef Z_SOLO
 #    if defined(__MWERKS__) && __dest_os != __be_os && __dest_os != __win32_os
