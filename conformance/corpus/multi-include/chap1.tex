\chapter{Introduction}

This report is split across two separate source files that the root document
assembles with \verb|\include|. The measured results are collected later in
Chapter~\ref{chap:results}, whose number is only known after the cross-reference
has been resolved on a second pass.

\section{Background}

The layered-transport problem motivates the measurements gathered in the
following chapter. Each chapter begins on a fresh page, so the assembled
document spans several pages.
