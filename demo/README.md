# demo/

A minimal static page — paste LaTeX, get a PDF — that also serves as the CI
smoke test. Playwright drives it in Chromium (primary; Electron-equivalent),
with Firefox and WebKit as advisory runs.
