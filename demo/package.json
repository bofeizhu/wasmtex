{
  "name": "wasmtex-demo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "WasmTeX runtime-v1 demo + Playwright PDF smoke (CI vehicle, not the npm package). Drives the wasmtex runtime; no vendored glue.",
  "license": "MIT",
  "scripts": {
    "serve": "node serve.mjs",
    "build:runtime": "npm --prefix ../runtime run build",
    "pretest": "npm --prefix ../runtime run build",
    "test": "playwright test",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
