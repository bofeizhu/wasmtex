# M2 — Rebase to TeX Live 2026: execution plan

Status: **COMPLETE 2026-07-24** — all acceptance checks verified
independently by the tester agent via `make rebase-check` (6/6 gates,
54.8 s; see docs/LOG.md milestone entry). (Originally opened
2026-07-23 after M1 closure; loop runs through M3 per the standing
push-through directive.) Port the build to a pinned
TL 2026 snapshot — the project's first annual rebase, and its founding
differentiator (DESIGN §1: "current TeX Live"). `luahbtex` exits the
multicall link, formats, and the rebase surface here (§9 amendment).
`build/upstream/` dissolves into our own build tree (§4 staging note).

## Acceptance (DESIGN §9 M2 bullet, as amended)

- Corpus seed documents compile through the runtime/demo against the
  TL 2026 artifacts: hello-world per engine (xetex, pdftex), one
  bibtex8 document, one makeindex document.
- The native flow's gates stay green: execution gate (banner updated to
  TL 2026 — a build-side pin, not runtime code), demo smoke 4/4,
  runtime suite green with regenerated fixtures, license audit green.
- `luahbtex` and the lua* formats absent from dist/; the multicall
  carries xetex, pdftex, bibtex8, xdvipdfmx, makeindex, kpsewhich.
- The rebase is captured as a scripted, documented operation (the
  DESIGN §6.2 `make rebase` seed — honest about what is still manual).
- M3's gates re-validate the rebased tree immediately after (recorded
  trade-off from the swap amendment).

## Ordered work list (one checkbox ≈ one reviewable commit)

- [x] **1. Plan.** This file. (Announced 2026-07-23.)
- [x] **2. Pin research + pins.lock.** Resolve the TL 2026 snapshot:
  texlive-source tag (expect `texlive-2026.0`) + the frozen
  `texlive2026-*.iso` from a TUG *historic* mirror (never rotating
  CTAN), sha256 + published-checksum cross-check, cache fetched and
  verified via fetch.sh; keep expat/fontconfig pins unless TL 2026
  demands newer. Record what upstream busytex never tested — from here
  the build config is OURS.
- [x] **3. Dissolve `build/upstream/`.** Fork the vendored Makefile
  into `build/engines/` as our maintained build config (MIT,
  derived-from-busytex provenance headers per DESIGN §2.1): drop
  luahbtex/lua53 from the multicall link, drop biber/cosmopolitan/
  arXiv/ubuntu-bundle paths, keep the §3 program set. Update
  build-native.sh to drive it; retire the M0-only staging area and its
  DESIGN §4 note in the same commit; PROVENANCE.md posture moves to
  per-file derived-work headers; THIRD_PARTY_NOTICES updated. The
  parked amd64 container flow gets a one-line pointer (fully re-pinned
  at M3, arm64).
- [x] **4. Build against TL 2026.** *(Landed 2026-07-24: gate green
  with the TL 2026 banner, emsdk KEPT at 3.1.43, both macOS patches
  retired as fixed-upstream, three Makefile-layer drift fixes. Known
  blocker carried to the next unit: ICU 78 converter-alias load
  regression breaks wasm XeTeX at font-manager init — root-caused and
  journaled; pdfTeX fully functional. **RESOLVED 2026-07-24 (item 4b):
  pointer-TOC packaging vs ICU 78's new length gate; fixed by
  genccode'ing the offset-TOC .dat into one portable C blob. All 10
  blocked tests green: runtime 186/186, smoke 4/4.**)*
  prep→native→basic→wasm→bundle→dist
  on the new pins. Expect drift: TL 2026's newer vendored harfbuzz/
  icu/freetype vs emsdk 3.1.43's clang — the emsdk-bump decision rule
  is: bump ONLY if the build genuinely requires it, to an exact pinned
  version, recorded in pins.lock + native-host.md + toolchain README
  in the same commit (the M3 container re-pin then inherits it).
  Re-test the two TARGET_OS_MAC patches against TL 2026's libpng/zlib
  (drop if fixed upstream, re-diff if drifted). Journal every
  failure→fix in a new `docs/plans/M2-journal.md`. Execution gate
  banner updated to TL 2026. Acceptance: dist/ assembles, gate green.
- [x] **5. Formats + dist pruning.** *(Verified 2026-07-23 — a deliberate
  re-check of items 3/4's output, no rebuild. dist/formats = exactly
  {xelatex,pdflatex}.fmt, lua-free; assets.json 7 assets / 6 roles, all
  classified, SHA256SUMS verified. A node MEMFS probe listed the mounted
  TL 2026 TDS and CONFIRMED the FORMAT_* constants
  (/texlive/…/web2c/{xetex/xelatex,pdftex/pdflatex}.fmt exist; no lua
  engine dirs) — the M1 carry-forward is CLOSED. The ~4.2 MB .fmt drop is
  NOT compression (both years gzip'd) nor a fmt-format change (W2TX
  identical); the decompressed content shrank ~9-10 MB each — benign.)*
  xelatex/pdflatex (+ whatever the
  basic scheme's retained non-lua set is); lua* formats pruned from
  the dump/prune step; assets.json reclassifies for free (structural
  roles — the rebase-proofing payoff). Runtime FORMAT_* MEMFS
  constants re-verified against the TL 2026 TDS (the M1 acceptance
  carry-forward: conformance catches a layout change — check it
  deliberately here).
- [x] **6. Fixture regeneration.** *(Landed 2026-07-23. All 21 fixtures
  (12 diagnostics + 9 sequencing) re-captured from the TL 2026 dist via a
  throwaway generator. Version-agnostic claim VALIDATED: parser+detector
  tests pass with ZERO test-file changes; every anchored substring + all
  exit codes held; the 3 bibtex8 fixtures are byte-identical to TL 2023.
  Churn was cosmetic (banners 2023→2026, LaTeX2e/L3/class dates,
  makeindex 2.17→2.18, render byte counts). ONE benign structural delta:
  TL 2026 stopped auto-loading ts1cmr.fd — a balanced (…) that never sits
  on the parser's paren stack, so no detector finding. Fixed the
  GENERATOR.md under-specification (added exact source documents). Gates:
  typecheck clean, 186/186, smoke 4/4, execution gate + license audit
  green.)*
  Re-capture diagnostics + sequencing
  fixtures per their GENERATOR.md files against TL 2026 engines;
  runtime suite green (rerun markers and diagnostic shapes are
  version-agnostic by design — this is the first real test of that
  claim; divergences are findings, not failures, and get journaled).
- [x] **7. Conformance seeds.** *(Landed 2026-07-23. Four seeds
  (hello-xetex, hello-pdftex, bib-cite, idx-makeindex) + expectations.json
  each; `conformance/run.mjs` drives the PUBLIC `createTypesetter` over the
  real wasm via a NEW esbuild Node-harness bundle `runtime/dist/node-harness.mjs`
  (the Node-delivery twin of `dist/worker.js`; the in-process adapter + node
  loader were promoted `test/support/` → `runtime/node/` and are now shared
  with the integration test — one `createNodeWorkerFactory`, zero duplication).
  Assertions per §8: exit code, page count (`countPages` — page-tree `/Count`
  cross-checked vs. leaf `/Type /Page`), space-stripped text snippets + absent
  neg-controls, exact diagnostics shape. The PDF text probe is factored into
  `conformance/pdf-probe.mjs`, shared with the demo smoke. **idx-makeindex is
  the first real-wasm makeindex exercise**: `engine → makeindex → engine →
  engine → xdvipdfmx`, the index renders (proof terms absent from the body).
  Guarded (green-skips without dist/); guarded `conformance` CI job added.
  Finding: bib-cite carries 4 pre-bibtex warnings the multi-pass log retains —
  pinned as the diagnostics shape. Gates: typecheck clean, runtime 186/186,
  demo smoke 4/4, conformance 4/4, license audit green.)*
  `conformance/` gains the seed corpus
  as committed documents + a small node runner driving the PUBLIC
  runtime against dist/: hello-xetex, hello-pdftex, bibtex8 doc,
  makeindex doc — assertions per §8 (exit code, PDF validity, text
  probe where sound). Wired as a guarded npm script (skips without
  dist/, like the integration tests). Demo smoke stays green.
- [x] **8. Rebase runbook.** `docs/rebase.md`: the scripted operation
  DESIGN §6.2 promises — pin update, fetch, patch re-test, build,
  fixture regen, corpus, gates — with the honest manual-step list and
  a `make rebase-check` convenience target if it earns itself. This is
  the artifact the whole milestone exists to seed.
- [x] **9. Acceptance run (PASSED 2026-07-24).** Tester agent independently executes the
  acceptance list (single entry point: `make rebase-check`); results +
  dist inventory into docs/LOG.md; RETIRE the TL 2023 pins.lock blocks
  (the named-owner step per docs/rebase.md 1d — the rebase has landed);
  mark M2 complete; continue to M3 per the push-through directive.

## Risks / standing decisions

- **emsdk 3.1.43 vs TL 2026 C++.** Newer vendored harfbuzz/icu may
  need a newer clang/libc++. Decision rule in item 4; a bump cascades
  into M3's container re-pin (already anticipated by the swap
  amendment) and must not be silent.
- **Upstream never built TL 2026.** The busytex Makefile's TL build
  targets may not survive texk/ build-system reorg between 2023 and
  2026 — this is the expected patch-by-patch grind (PROMPT.md M1
  original text). Journal discipline is the deliverable as much as
  the build.
- **ISO availability.** TL 2026 must come from the TUG historic
  archive (frozen); if the 2026 ISO is not yet in historic/ (release
  year), pin the exact dated ISO from the release area with its
  published checksum and note the mirror-rotation exposure + a
  re-pin-at-M3 reminder.
- **LuaTeX removal scope.** Dropping luahbtex also drops lua53 and
  LuaJIT-adjacent deps from the wasm link — verify no other program
  links them (kpathsea tools don't); smaller wasm is expected and
  budget-noted, not silently absorbed.
- **Fixture churn.** Engine banners and log cosmetics WILL change;
  the parser/machine detectors are version-agnostic substrings — any
  detector that breaks is a real finding for the annual-rebase log.
