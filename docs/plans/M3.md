# M3 — Build logistics & CI: execution plan

Status: **COMPLETE 2026-07-23** — all (revised, post-descope)
acceptance checks green; independently accepted by the tester and
confirmed on the current HEAD (CI run 30016715455: artifacts-build +
conformance + demo-smoke all success, 0 deprecation warnings). This was
the loop's STOP-TARGET milestone per the standing push-through
directive, so the autonomous loop STOPS here.

Scope note: two acceptance items were deliberately DROPPED mid-milestone
by user decision (2026-07-23, DESIGN §6.1/§9 amendments) — byte-for-byte
reproducibility and the three-way hash-equivalence check. The build is
formalized around the surviving bar: a pinned **arm64** Linux container
is the canonical builder, CI runs the full wasm path AND the functional
gates against the produced artifact, and host-arch/platform
independence is settled FUNCTIONALLY (the arm64-built wasm runs the
conformance corpus on amd64 and macOS arm64). docs/plans/M3-notes.md
carries the accumulated findings this plan is built on.

## Acceptance (DESIGN §9 M3 bullet + accumulated deferrals)

- A pinned arm64 Linux container (digest-pinned userland + emsdk
  3.1.43, image ID in pins.lock) builds the complete artifact set;
  all gates green against its dist/ (execution gate, runtime real-wasm
  tests, conformance 4/4, demo smoke 4/4).
- ~~**Build-twice reproducibility**~~ **DROPPED 2026-07-23** (user
  decision; DESIGN §6.1 + §9 amendments): byte-identity is no longer a
  v1 requirement. The one CI repro run that ran proved the engine +
  formats are byte-identical and isolated the only divergence
  (install-tl wall-clock timestamps in generated map files) — recorded
  as a known, deliberately-unfixed gap. Functional correctness is the
  release bar instead.
- ~~**Three-way equivalence**~~ **REPLACED 2026-07-23** with a
  FUNCTIONAL cross-platform check (byte-hash equivalence dropped with
  the repro gate): the arm64 container-built wasm runs the conformance
  corpus on other platforms. Already confirmed on macOS arm64
  (execution gate + all 4 corpus entries green, 2026-07-23). No amd64
  lane needed.
- **CI runs the wasm path**: GitHub Actions builds in the pinned
  container (arm64 runner) and executes the full gate set on the
  produced artifacts — closing the M1/M2 carry-forward that CI proves
  only the pure modules.
- ISO re-pinned to the TUG historic archive if TL 2026 has landed
  there (the standing lock reminder); otherwise the reminder stands
  with a dated re-check note.
- CLAUDE.md/PROMPT.md's "CI green" now means the real thing; loop
  STOPS with the final summary + push notification.

## Ordered work list (one checkbox ≈ one reviewable commit)

- [x] **1. Plan.** This file. (Announced 2026-07-23.)
- [x] **2. ISO historic re-pin check.** *(Done 2026-07-23: historic/2026/
  still lacks the consolidated ISO — 404; dated re-check note added to
  the lock; reminder stands.)* Probe
  `…/historic/systems/texlive/2026/` for the consolidated ISO; if
  present, swap url+checksum_url per the lock's re-pin instruction
  (content hashes unchanged — self-verifying); if absent, date the
  re-check in the lock comment and move on.
- [x] **3. arm64 container.** `build/toolchain/Dockerfile` re-pinned:
  arm64 ubuntu digest (decide 22.04-vs-24.04 with rationale), emsdk
  3.1.43 linux-arm64 at the same pinned commit, prerequisites per the
  current build/engines needs (smaller than M0's set — no upstream CI
  mirroring); build + smoke (emcc version, aarch64); image ID pinned
  additively in pins.lock ([toolchain-image-arm64]; the amd64 block
  stays for the equivalence lane). Provenance headers; native-host.md
  and toolchain README updated.
- [x] **4. Containerized build green.** Re-point the parked container
  flow (build.sh + run-in-container.sh — the STALE-PATH banners come
  off here) at build/engines + the 2026 pins + the arm64 image:
  offline (--network none), cache ro-mounted, same stage sequence as
  build-native.sh, `make artifacts-container` drives it. Full build
  inside the container lands dist/ + all gates green against it.
  Fold in: drop `--use-preload-cache` from the bundle step (the
  M1-journaled IndexedDB always-on cache deviation — §5.2 optional-
  adapter posture restored; demo/runtime unaffected, storage-less
  path already proven).
- [x] **5. Reproducibility double-build → DESCOPED 2026-07-23.**
  The gate (`build/repro-check.sh`) and the ls-R ordering fix
  (`build/engines/normalize-lsr.py`) landed and one CI repro run
  executed (run 30003309704): engine + `.fmt` byte-identical, sole
  remaining divergence = install-tl wall-clock timestamps in generated
  map files. Rather than build the timestamp normalizer, the user
  dropped byte-identity from v1 (DESIGN §6.1/§9 amendments). Closed as
  descoped: the harness + ls-R fix stay (behavior-preserving, zero
  cost); the timestamp gap is a documented known-limitation. No further
  repro work; the planned parallel-matrix redesign is CANCELLED.
- [x] **6. Cross-platform check → FUNCTIONAL, DONE 2026-07-23.**
  Byte-hash equivalence dropped with the repro gate. Replaced by:
  run the arm64 container-built wasm under the conformance corpus on a
  different platform. DONE — the CI-built `dist/` (Linux arm64) passed
  the execution gate + all 4 corpus entries under node on macOS arm64.
  Platform-independence settled functionally; no amd64 container lane.
  *(Original hash-equivalence text retained below for history.)* Build
  on {arm64 macOS native (exists), arm64 container (item 4/5 artifacts
  on disk), amd64 container}; compare per-artifact hashes; record the
  verdict in the journal + M3-notes: if wasm/artifacts are
  bit-identical across arches, amd64 is demoted to an optional lane;
  any divergence is analyzed (the .fmt native-dump suspicion from the
  original analysis) and drives what the canonical builder must pin.
- [x] **7. CI wires the wasm path. DONE 2026-07-23** — verified green
  in CI: run 30012659864's `artifacts-build` + `conformance` +
  `demo-smoke` jobs ALL succeeded; the functional gates run for real
  against the CI-built artifact. (Node-20 deprecation warnings on the
  artifact/cache actions cleared in a follow-up: upload@v6, download@v7,
  cache@v5 — the node24 majors; note download's node24 major is v7 not
  v6.) *(Slice A DONE 2026-07-23: pinned
  image pushed to GHCR, registry digest pinned, toolchain-smoke green
  on ubuntu-24.04-arm — private-package pull works with the Actions
  token, identity gate passed, and the runner measures 109 GB free
  disk, obsoleting the 14 GB macOS-derived constraint; see M3-notes
  correction.)* Remaining, slice B: pinned-container build on the
  arm64 Linux runner (full ISO staging is fine — selective staging
  demoted to optional), ISO acquisition strategy (~6.8 GB fetch vs
  actions/cache 10 GB quota), artifact caching keyed by
  pins.lock+engines-config hash so PR runs reuse cached dist/ while
  main pushes (or a schedule) rebuild; the guarded demo-smoke/
  conformance/integration jobs flip to actually running against the
  built artifact — the functional release bar (§6.1 amendment; NO repro
  matrix, that work is cancelled). Measure and journal disk/time
  budgets. *(Slice B1 DONE 2026-07-23: artifacts-build.yml produces +
  gates dist/ in CI, green; remaining is wiring the guarded gate jobs
  to consume its uploaded artifact.)* *(Slice B2 DONE 2026-07-23:
  conformance + demo-smoke moved to artifacts-build.yml as needs:build
  jobs that download the same-run artifact and run on ubuntu-latest
  [amd64 → also functionally proves the arm64-built wasm runs on amd64];
  the green-skip stubs retired from build.yml; reviewer-approved,
  all-five-artifact assert added. Item 7 complete pending the first
  green gate run. Accepted follow-up: per-PR cross-run integration
  coverage in build.yml — deferred, runtime unit tests cover those PRs.)*
- [x] **8. Acceptance run. ACCEPT 2026-07-23.** Tester independently
  verified the revised acceptance list — every criterion PASS: pinned
  arm64 container built the full artifact set in CI with the
  in-container execution gate green (identity gate matched image_id
  5d4af653…, offline, TeX Live 2026); the functional gates ran FOR REAL
  (no green-skip — all-five assert + "all 4 corpus entries passed" +
  demo "4 passed") against the CI artifact; cross-platform proven
  firsthand (execution gate + conformance 4/4 re-run on macOS arm64,
  plus the gates on amd64 in CI); NO repro/hash-equivalence gate;
  ISO re-pin reminder present; local license-audit + 186/186 runtime
  green. Confirmed on the current HEAD (run 30016715455, all jobs
  success, 0 deprecation warnings). **M3 COMPLETE — the loop STOPS.**

## Risks / standing decisions

- ~~**Repro check is a parallel matrix**~~ **SUPERSEDED / CANCELLED
  2026-07-23** (same day, hours later): the repro gate itself was
  dropped from v1 (§6.1 amendment — byte-identity is no longer a
  requirement), so there is nothing to parallelize. The parallel-matrix
  redesign is not built. `build/repro-check.sh` and
  `build/engines/normalize-lsr.py` stay in-tree (behavior-preserving,
  zero-cost, already committed) as available tooling if byte-repro is
  ever revived post-1.0; neither runs in the release path.

- **No container builds on the local machine** (user directive
  2026-07-23, issued mid-item-5): neither the arm64 Linux nor the
  amd64 toolchain container runs here again. The two completed local
  container builds (item 4's and item 5's build #2) are the standing
  evidence base; every subsequent container build — item 6's amd64
  lane, any repro re-run, item 8's acceptance rebuild — happens on CI
  runners (item 7's infrastructure). Native macOS builds
  (`make artifacts`) remain unrestricted. This re-sequences the tail
  of the milestone: item 7's CI build capability becomes the
  prerequisite for finishing item 6's amd64 lane, so expect item 7
  (or its build-workflow slice) to land before item 6 closes.

- ~~**Runner disk (14 GB)** is the hard wall~~ **RETIRED 2026-07-23**:
  measured 109 GB free on ubuntu-24.04-arm (the 14 GB figure was the
  macOS runner spec — M3-notes correction). Everything coexists with
  margin; selective staging is now an optional optimization. The live
  slice-B questions are ISO acquisition (per-run 6.8 GB download vs
  actions/cache quota) and wall-time on 4 vCPU.
- **Container build wall-time** on 4-vCPU arm64 runners: local M-series
  took ~70 min; expect 2-3× in CI. Within the 6 h job cap, but
  caching must make it a rare event (main/schedule, not per-PR).
- **Nondeterminism discoveries** at item 5 are findings, not
  surprises: gzip mtime in .fmt (gzip -n equivalent?), archive
  member ordering, and locale sorting are the classic suspects; fix
  at the source, never by fuzzy comparison.
- **The macOS lane** of the equivalence check uses the DEV toolchain
  (documented-not-pinned host tools) — hash differences attributable
  to the host layer (vs the container pair) are expected findings,
  not failures; the container pair is the canonical comparison.
- **emsdk linux-arm64 at 3.1.43**: assumed available (emsdk published
  arm64 Linux binaries in that era); if the prebuilt is missing, the
  decision rule mirrors M2's: minimum exact bump, pinned everywhere,
  journaled — and the native path re-verifies against it.
