# M4 — Bundles + manifests: execution plan

Status: ACTIVE (opened 2026-07-24, after M3 closed). M4 turns the single
`texlive-basic` data bundle into **tlpdb-driven tiers** (`core` /
`extended` / `full`), emits a **top-level integrity manifest** with a
per-bundle provided-package index, and implements the §5.4 **on-demand
resolution** the runtime API already forward-declares (typed but inert).
This is the milestone that makes the `bundles: { preload, onDemand }`
config real. Grounded in the 2026-07-24 codebase map (docs/LOG.md).

## What already exists (leverage, don't rebuild)

- **One bundle:** `texlive-basic.{js,data}` = install-tl `scheme-basic`
  + `collection-xetex` + `collection-latex`, file_packager'd (LZ4,
  52.7 MB). The `build/texlive-%` Makefile rule is generic — only ever
  run with `%=basic`.
- **tlpdb on disk, read by nothing:** `source/texmfrepo/tlpkg/texlive.tlpdb`
  (ISO-staged). The exact `package → files` map lives here; nothing
  parses it. Current provided-package signal is a crude
  `\ProvidesPackage` grep dumped as dead JS comments atop the bundle
  `.js` (`build/engines/Makefile:601`).
- **`assets.json` schemaVersion 1** (`build/manifest/gen-assets.mjs`):
  full per-file `{path,bytes,sha256,role}` inventory, deterministic.
  Its header + README explicitly call it the M4 manifest precursor.
- **`build/bundles/`** exists as a README-only stub.
- **Runtime `onDemand` plumbing** is typed + validated + copied but
  never consumed; `stats.bundlesLoaded` just echoes `preload`.
  Diagnostics carry the missing filename verbatim in `message` (a
  contract `diagnostics.ts` promises M4) but not as a structured field.
- **Corpus** (4 entries) stays entirely inside basic — no test yet
  forces a second tier.

## Design decisions (this plan commits to these; open ones flagged)

- **Tiers are DISJOINT file sets.** Every TL file lands in exactly one
  of core/extended/full, so on-demand bundles mount on top of core
  without file collisions. Assignment is by tlpdb collection → tier.
  - **`core`** = today's basic content (scheme-basic + xetex + latex
    collections) + the retained `.fmt`s. The always-preloaded tier;
    `texlive-basic` is renamed to `core` (back-comptible alias kept one
    release — see risks).
  - **`extended`** = a curated popular set the tlpdb pulls in:
    `collection-latexrecommended`, `-latexextra`, `-mathscience`,
    `-fontsrecommended`, `-langcjk`/`-xetex` CJK support (`ctex`,
    `xeCJK` — DESIGN §6.3), `unicode-math`. Sized to stay useful but
    bounded.
  - **`full`** = everything else we ship from TL (≈ scheme-full minus
    doc/src, minus core+extended). **Large (multi-GB) — see risks;**
    may be built on a separate schedule, not the per-push path.
- **Manifest:** evolve `assets.json` → **`manifest.json`
  (schemaVersion 2)**, superset-compatible: keep the per-file
  inventory, ADD `texliveSnapshot` (the TL 2026 freeze id + tlpdb
  revision), `engines` (named list), and `bundles: [{ name, files[],
  bytes, provides: [package…] }]`. The runtime consumes this; hosts
  verify installs against it (DESIGN §7). Keep `assets.json` as a
  generated alias for one release for back-compat.
- **On-demand mount:** load a tier's file_packager `.data` into MEMFS
  **after** engine init (not only preRun). **Open technical risk:**
  confirm Emscripten's `runWithFS`/`installDataPackage` works
  post-init in our worker; if the packaged loader assumes preRun only,
  fall back to WORKERFS or a re-init-with-more-preloads path. Item 5
  resolves this with a spike before the full runtime wiring.
- **§5.4 resolution, both paths:** (a) static `\usepackage` /
  `\RequirePackage` scan of project sources (skip project-local
  `.sty`/`.cls`) → map names via the manifest `provides` index → ensure
  those bundles; (b) on a pass failing with kpathsea `File \`x' not
  found` for a name the manifest maps to an unloaded on-demand bundle
  → load it, retry once. No network anywhere.

## Acceptance (DESIGN §5.4 + §7 + §9 M4 bullet)

- The build produces **≥3 disjoint tier bundles** (core/extended/full)
  from the pinned tlpdb — core equivalent to today's basic, extended
  and full strictly additive — each a file_packager `.js`+`.data`.
- **`manifest.json` (schemaVersion 2)** carries the TL snapshot id, the
  engine list, and a per-bundle `provides` index derived from the
  tlpdb; every file has `{bytes, sha256}`; it verifies against
  `SHA256SUMS`; deterministic under `SOURCE_DATE_EPOCH`.
- The runtime **loads on-demand bundles for real**: `bundlesLoaded`
  reflects actually-mounted tiers; `preload: ['core']` +
  `onDemand: ['extended','full']` works cold and storage-less.
- **§5.4 both paths pass tests:** a corpus/fixture doc that
  `\usepackage`es something only in `extended` typesets green via (a)
  the static scan, and another via (b) the missing-file retry (scan
  disabled) — proving the retry mechanism independently.
- **CI**: the tiered build runs in the pinned container; the functional
  gates run against the tiered artifact; sizes are budgeted and
  journaled (engine wasm, core, extended, full).
- Provenance/reproducibility bars unchanged (MIT-only, pinned inputs;
  byte-repro remains descoped per §6.1 amendment).

## Ordered work list (one checkbox ≈ one reviewable commit)

- [x] **1. Plan.** This file. (Announced 2026-07-24.)
- [ ] **2. tlpdb parser + tier map.** `build/bundles/`: a zero-dep
  parser of `texlive.tlpdb` → `{package: {files[], depend[], provides[]}}`
  and a committed tier-definition (collection → tier) that resolves to a
  disjoint `file → tier` map + per-tier `provides` index. Unit tests
  against the pinned tlpdb (stable counts, disjointness, every core file
  still present). No build change yet.
- [ ] **3. Multi-bundle build.** Generalize the Makefile/​drivers to
  install + file_packager each tier (core/extended/full) with disjoint
  `--preload` sets driven by item 2's map; `core` reproduces today's
  basic content. Emits `dist/{core,extended,full}.{js,data}`. CI
  container rebuild; measure sizes/time. (Full-tier handling per risks.)
- [ ] **4. manifest.json (schemaVersion 2).** Extend `gen-assets.mjs`:
  snapshot id + engine list + per-bundle `provides` index; keep the
  per-file inventory; `assets.json` alias retained. Update the runtime
  inventory consumer + protocol types for the new shape.
- [ ] **5. On-demand mount (worker).** Resolve the post-init
  data-package spike, then implement real on-demand tier loading in the
  worker; wire `stats.bundlesLoaded`; `onAssetProgress` for on-demand
  fetches. Runtime unit tests with a stub second bundle.
- [ ] **6. §5.4(a) static \usepackage scan.** Project-source scan →
  manifest `provides` → bundle preselection. Unit tests.
- [ ] **7. §5.4(b) missing-file retry.** Structured kpathsea
  `not found` filename extraction (diagnostics) → map to unloaded
  bundle → load + retry once. Unit tests proving the path independently
  of (a).
- [ ] **8. Beyond-basic corpus + integration.** New conformance
  entries that require `extended` (drive both resolution paths);
  manifest-verification test; end-to-end runtime integration.
- [ ] **9. Acceptance run.** Tester independently verifies the
  acceptance list against the CI-built tiered artifact; size budgets
  journaled; mark M4 complete; present summary. (Loop continuation is a
  user call — M5 begins release engineering + user-only publish steps.)

## Risks / standing decisions

- **`full` tier size + CI time is the marquee risk.** scheme-full is
  thousands of packages; its `.data` could be 1–4 GB and its install-tl
  run much longer than basic's. Options (decide at item 3, journal it):
  build `core`+`extended` on the per-push path and `full` on a
  scheduled/dispatch job; or cap `full` to a defined package set. The
  per-bundle-archive design (DESIGN §7) means hosts opt into `full`
  separately, so not shipping it by default is fine. **This is worth a
  user check before item 3 commits to building full in CI.**
- **On-demand post-init mount** (design note above) is the runtime
  technical unknown; item 5 spikes it first. If Emscripten can't mount
  a data package post-init in our setup, the fallback (WORKERFS or
  re-init) changes the §5.4 UX (a bundle load may cost a worker
  re-init) — acceptable but must be journaled.
- **Disjointness vs kpathsea:** a file present in `full` but referenced
  by a `core` doc must resolve only after the tier loads — the retry
  path (7) covers macro-driven refs the scan (6) misses. Tiers must not
  duplicate files (breaks the integrity manifest's one-file-one-hash
  assumption).
- **Reproducibility:** unchanged — byte-repro stays descoped (§6.1
  amendment); `SOURCE_DATE_EPOCH` + `normalize-lsr.py` keep the tiers
  mostly-deterministic for free. No local container builds.
- **Back-compat:** `texlive-basic.*` → `core.*` rename keeps a
  generated alias for one release so the demo/0.0.1 consumers don't
  break; drop at M5.
