# M5 — Release engineering + hardening: execution plan

Status: ACTIVE (opened 2026-07-24, after M4 closed). The FINAL milestone:
turn the accepted engine + runtime + bundle system into a real, verifiable,
documented **release** — versioned asset archives, an enumerated license
manifest for the shipped aggregate, size budgets, soak + browser-matrix
hardening, the fuller conformance corpus, and the docs an embedder needs —
then hand the actual tag / GitHub release / `npm publish` to the user
(those are user-only actions). DESIGN §9 M5 = "former M4 + M5"; PROMPT.md's
"M4 — Release engineering" + "M5 — Hardening" together are the concrete
checklist. Grounded in the 2026-07-24 readiness map (docs/LOG.md).

## Decisions this milestone needs (flagged; some are user calls)

- **[DECIDED 2026-07-24] First real release version = `0.1.0`** (user
  choice). Pre-1.0, signals the API may still change. Sets: npm
  `wasmtex@0.1.0`, GitHub tag `assets-v0.1.0`, archives
  `wasmtex-assets-0.1.0.tar.gz` / `wasmtex-bundle-{core,academic}-0.1.0.tar.gz`,
  all in lockstep. Baked into items 7–9.
- **[USER-ish] npm-consumer → asset discovery/verification.** The npm
  package ships JS only (~418 KB, no engine/bundles, by design §4); a
  consumer points it at separately-hosted assets via `assetsBaseUrl`. Today
  nothing ties `wasmtex@X` to its matching `assets-vX.Y.Z`. Recommendation:
  export an `ASSETS_VERSION` constant from the package and have
  `createTypesetter` SOFT-verify the fetched `manifest.json` matches (clear
  error on mismatch, overridable). Decide at item 3/8.
- **[proposed defaults] Size-budget thresholds.** Current: busytex.wasm
  27.5 MB, core.data 53.9 MB, core.js 1.47 MB, academic.data 496 MB.
  Proposal: budget the PRELOAD path strictly (busytex.wasm ≤ 30 MB, core.data
  ≤ 60 MB, core.js ≤ 2 MB) and academic loosely (≤ 550 MB, on-demand). Tune
  at item 5.

## Acceptance (DESIGN §7/§8/§9 M5 + PROMPT M4/M5 checklists)

- **Versioned archives** produced reproducibly from a built `dist/`:
  `wasmtex-assets-<v>.tar.gz` (engine + formats + all bundles + manifest),
  and per-bundle `wasmtex-bundle-core-<v>.tar.gz` / `-academic-<v>.tar.gz`;
  each archive's contents verify against `manifest.json`.
- **License audit of the shipped aggregate**: THIRD_PARTY_NOTICES.md
  enumerates the license of every TeX Live package actually in core+academic
  (from tlpdb `catalogue-license`); a CI check fails if a shipped package has
  no recorded license (or a disallowed one). The §7 aggregate-distribution
  statement stands.
- **Docs**: root README rewritten (accurate status), a real embedding guide
  (§10 profile — assetsBaseUrl, custom scheme, cold-start, integrity
  verification, no-network), runtime/README asset-channel language flipped.
- **Size budgets**: a checked-in budget + a CI check diffing manifest `bytes`
  against it; growth of the preload path fails loud.
- **Hardening**: soak test (50 sequential jobs w/ random cancellations — no
  cross-job contamination, memory returns to baseline after `dispose()`);
  browser matrix (chromium + firefox + webkit) incl. a REAL-browser on-demand
  academic mount; the fuller conformance corpus (unicode-math, multi-chapter
  `\include`, a known-bad doc, a standalone TikZ figure, a host-supplied-font
  CJK variant).
- **Release dry-run green**: `npm pack`/`--dry-run` produces the expected
  tarball; the release workflow builds + attaches archives on a tag — proven
  by a dry run / dispatch, WITHOUT publishing.
- **The release itself** (tag `assets-v<v>`, GitHub Release, `npm publish`)
  is executed by the USER; M5 is marked complete when the prep is green and
  the release is cut.

## Ordered work list (one checkbox ≈ one reviewable commit)

- [x] **1. Plan.** This file. (Announced 2026-07-24.)
- [x] **2. Bundle license enumeration + §7 audit. DONE 2026-07-24**,
  reviewer-approved. `build/bundles/licenses.mjs` (enumerator + fail-
  closed audit + CLI) + `license-exceptions.mjs` (22 packages resolved
  via LICENSE.TL, cited) + 23 tests. **All 2545 shipped packages free**
  (151 core + 2394 academic); 36 license tokens, none non-free. The
  audit FAILS closed on missing/nonfree/unknown (22 flagged raw: 17 TL-
  infra with no metadata + 5 CTAN grab-bags with a `collection` token —
  all TL-proper → LICENSE.TL). `dist/licenses.json` emitted before
  SHA256SUMS (hashed, manifest-listed, license-inventory role); audit in
  the dist stage + license-audit.sh check (f) + build.yml tests. **Kept
  all 22** (LICENSE.TL sound; some grab-bags carry journal packages like
  authblk/gensymb) — dropping the 5 from academic remains a one-line
  tiers.mjs option. Original: A generator (reusing
  `build/bundles/tlpdb.mjs` `catalogue-license`) that walks each tier's
  package set → a per-package license inventory; fold into
  THIRD_PARTY_NOTICES.md (retire the stale "to be inventoried" deferral);
  add a fail-closed CI check for a shipped package lacking a recorded license
  or carrying a disallowed one. Version-independent.
- [x] **3. Docs: README + embedding guide. DONE 2026-07-24.** Root
  README rewritten (accurate M0–M4 Done / M5 in-progress status, 0.1.0
  imminent); `docs/embedding.md` (~525 lines, the §10 profile end to
  end — hosted-assets split, custom scheme via locateAsset+workerUrl,
  cold-start storage-less, HOST-side manifest integrity verify, the
  bundles/§5.4 model, job API); runtime/README flipped (assets ship as
  GitHub Release archives) + Node floor corrected to 18 (24 is the
  tested major, not the minimum). APIs grep-verified vs client.ts; fixed
  fontspec(→academic)/tikz(→pgf)/locateAsset-not-worker. **Item-8 spec
  surfaced:** manifest.json needs a lockstep package `version` field for
  ASSETS_VERSION to verify against (has schemaVersion + texliveSnapshot,
  no lockstep version yet). Original: Rewrite the stale root README
  status; write `docs/embedding.md` (the §10 hard-constraint profile end to
  end); flip runtime/README's "no release channel yet"; reconcile
  `engines.node` vs the "Node 24" claim. Sketch the ASSETS_VERSION discovery
  convention (coded in item 8). Version-independent.
- [ ] **4. Fuller conformance corpus.** Add: `unicode-math` (XeTeX), a
  multi-chapter `\include` project, a known-bad document (asserts the
  diagnostics shape, not a PDF), a standalone TikZ figure, and a
  host-supplied-font CJK variant (§6.3: font passed via `files`, not
  bundled). Version-independent.
- [ ] **5. Size budgets.** A checked-in `build/budgets.json` (preload-path
  thresholds; academic loose) + a checker that reads `dist/manifest.json`
  `bytes` and fails on breach; wire into CI (the artifacts-build gate and/or
  a fast job reading the manifest). Version-independent.
- [ ] **6. Soak + browser matrix.** The soak test (50 jobs, random cancels,
  no contamination, memory-to-baseline after dispose — Node integration; a
  browser variant if feasible). Add firefox + webkit Playwright projects; a
  real-browser on-demand academic-mount smoke; migrate the demo off the
  `texlive-basic` alias → `preload:['core'], onDemand:['academic']`.
  Version-independent. (The alias is dropped from the build here or at item 8.)
- [ ] **7. Versioned-archive tooling.** A `make pack` / `build/release/` that
  produces `wasmtex-assets-<v>.tar.gz` + the per-bundle archives from a built
  `dist/`, deterministically, each verifying against `manifest.json`; a
  release-notes template carrying the §7 aggregate statement. NEEDS the
  version (item-8 decision) for filenames — parameterize on `<v>` so the tool
  is version-agnostic and the tag supplies it.
- [ ] **8. Release workflow + version lockstep.** `.github/workflows/
  release.yml` triggered by an `assets-v*` tag: build in the pinned
  container, pack the archives (item 7), attach them to the GitHub Release,
  run the full gate set. Embed `ASSETS_VERSION` (0.1.0) in the npm
  package + the soft manifest-version verify (item-3 convention). **Also
  ADD a lockstep package `version` field to manifest.json** (gen-assets)
  for ASSETS_VERSION to check against — it has schemaVersion +
  texliveSnapshot but no package-lockstep version yet (item-3 finding).
  Prove via dispatch/dry-run WITHOUT publishing.
- [ ] **9. npm publish prep + dry-run.** Bump `wasmtex` to the chosen version
  (lockstep), `npm pack` + `--dry-run`, verify the tarball contents/size;
  final acceptance. The actual `npm publish` + `git tag` + GitHub Release are
  USER-ONLY — hand off with exact commands.
- [ ] **10. Acceptance + release.** Tester independently verifies the M5
  acceptance list; the USER cuts the release (tag → release → publish);
  record results; mark M5 complete — **WasmTeX v1 shipped.**

## Risks / standing decisions

- **User-only actions gate the finish.** Tag, GitHub Release, and
  `npm publish` are the user's calls (standing rule since M2). M5 does ALL
  prep + a dry run; the release moment is a handoff, not autonomous.
- **License enumeration correctness is load-bearing** (it's a legal
  statement about what we ship). `catalogue-license` can be missing/`noinfo`
  for some packages — the audit must FAIL on those, not guess, so a human
  resolves each before release. This may surface packages we must document
  or drop from `academic`.
- **No local container builds** (standing directive) — release-archive
  packing runs on a built dist (native for dev, container for the real
  release via item 8's workflow). The versioned archives that ship come from
  the pinned container only (DESIGN §9 floor).
- **texlive-basic alias drop** is a breaking change for any 0.0.1 consumer
  that used it — acceptable pre-1.0 (name-reservation release), noted in the
  release notes; the demo migrates to core+academic in the same change.
- **Reproducibility stays descoped** (§6.1 amendment); archives are packed
  deterministically (sorted, SOURCE_DATE_EPOCH) but not gated byte-identical.
