# runtime/test/

The runtime unit tests (DESIGN.md §8): protocol correlation (late/foreign
`jobId` ignored), cancellation (in-flight kill, next job clean), bundle
resolution including the missing-file retry, the diagnostics parser, and
manifest verification.
